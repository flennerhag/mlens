language: python

matrix:
    include:
        - os: linux
          sudo: required
          python: 3.6
        - os: linux
          sudo: required
          python: 3.5
        - os: linux
          sudo: required
          python: 2.7

install:
  - set -e
  - mlens/.travis/install.sh

script:
  - set -e
  - nosetests -s -v --with-coverage --exclude mlens/visualization --exclude mlens/externals

after_success:
  - coveralls
